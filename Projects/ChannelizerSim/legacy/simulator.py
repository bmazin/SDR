import numpy
import matplotlib as mp
import random, math, sim_utilities




        
    
###########################################################################################
x = [random.gauss(0, 0.5) for i in range(samples)]
y = FIR_1_withPulse(x, 1e-9, 2*math.pi*10e6)
time = [i*2e-6 for i in range(500)]

for t in time:
    if t < 250*2e-6:
        template.append(0)
    else:
        template.append(math.exp(



mp.plot(f, abs(green_f[0][0:L/2]))
#mp.show()




